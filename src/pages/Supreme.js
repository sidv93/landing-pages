import React from 'react';
import PropTypes from 'prop-types';

const Supreme = props => {
    return (
        <div>
            Supreme
        </div>
    );
};

Supreme.propTypes = {
    
};

export default Supreme;